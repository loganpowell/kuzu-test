{
  "name": "@relish/schema",
  "version": "0.1.0",
  "description": "Schema validation and compilation for Relish authorization system",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:validator": "vitest run tests/validator.test.ts",
    "build:compiler": "cd compiler && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3",
    "vitest": "^1.1.1"
  }
}
